<!-- GENERATED by scripts/sweep-baselines.ts — do not hand-edit. -->
# Scripted-baseline parameter sweep

The two scripted baselines have three free numbers between them
(`homesteader.maxClaims`, `raider.maxClaims`, `raider.minYield`). They were
**searched, not guessed**: this table is a deterministic grid sweep over
5 seeded 18-turn 9-seat episodes of the
certification mix (five homesteaders, four raiders), run by
`src/game/tune.ts` and re-run in CI by `src/game/tune.test.ts`.

* **Objective** — the mean banked score (`Σ income + Σ salvage`, the number the
  league ranks by) of the seats playing the candidate, averaged over the seeds.
  The other baseline is held at the REFERENCE field
  (`homesteader {"maxClaims":3}`,
  `raider {"maxClaims":3,"minYield":2}` — the pre-tuning numbers), so every row
  of every table is comparable and tuning one policy cannot move the other's table.
* **Seeds** — `[1,7,13,29,101]`; **grid** — `maxClaims ∈ [1,2,3,4,5,6,7,8]`,
  `minYield ∈ [1,2,3]`.
* **Selection rule** — the SMALLEST parameters whose mean is within
  3 % of the grid's best. Parsimony is the tie-break because these are
  certification/filler policies: a smaller order set is cheaper on the wire and in
  the prompt, and the design note's baseline contract is a short order list. Above
  `maxClaims = 3` the curve is flat (the extra claims are rarely affordable), so
  the band is where the real answer lives, not the raw argmax.

Shipped: `homesteader {"maxClaims":3}`, `raider {"maxClaims":4,"minYield":3}`.

## homesteader (raiders held at the reference field, scored on seats 0-4)

| parameters | mean banked | vs best | selected |
|---|---|---|---|
| `maxClaims=1` | 136.88 | -34.1 % |  |
| `maxClaims=2` | 177.40 | -14.5 % |  |
| `maxClaims=3` | 202.28 | -2.5 % | **selected** · shipped |
| `maxClaims=4` | 204.76 | -1.3 % |  |
| `maxClaims=5` | 206.40 | -0.6 % |  |
| `maxClaims=6` | 207.16 | -0.2 % |  |
| `maxClaims=7` | 207.56 | 0.0 % | argmax |
| `maxClaims=8` | 207.52 | -0.0 % |  |

## raider (homesteaders held at the reference field, scored on seats 5-8)

| parameters | mean banked | vs best | selected |
|---|---|---|---|
| `maxClaims=1 minYield=1` | 143.45 | -23.4 % |  |
| `maxClaims=1 minYield=2` | 145.05 | -22.5 % |  |
| `maxClaims=1 minYield=3` | 144.60 | -22.7 % |  |
| `maxClaims=2 minYield=1` | 159.05 | -15.0 % |  |
| `maxClaims=2 minYield=2` | 159.45 | -14.8 % |  |
| `maxClaims=2 minYield=3` | 170.55 | -8.9 % |  |
| `maxClaims=3 minYield=1` | 166.25 | -11.2 % |  |
| `maxClaims=3 minYield=2` | 164.20 | -12.3 % |  |
| `maxClaims=3 minYield=3` | 180.80 | -3.4 % |  |
| `maxClaims=4 minYield=1` | 169.25 | -9.6 % |  |
| `maxClaims=4 minYield=2` | 171.25 | -8.5 % |  |
| `maxClaims=4 minYield=3` | 181.80 | -2.9 % | **selected** · shipped |
| `maxClaims=5 minYield=1` | 172.40 | -7.9 % |  |
| `maxClaims=5 minYield=2` | 170.15 | -9.1 % |  |
| `maxClaims=5 minYield=3` | 185.20 | -1.0 % |  |
| `maxClaims=6 minYield=1` | 171.95 | -8.1 % |  |
| `maxClaims=6 minYield=2` | 171.65 | -8.3 % |  |
| `maxClaims=6 minYield=3` | 186.75 | -0.2 % |  |
| `maxClaims=7 minYield=1` | 172.40 | -7.9 % |  |
| `maxClaims=7 minYield=2` | 172.00 | -8.1 % |  |
| `maxClaims=7 minYield=3` | 187.10 | -0.0 % |  |
| `maxClaims=8 minYield=1` | 172.40 | -7.9 % |  |
| `maxClaims=8 minYield=2` | 171.90 | -8.1 % |  |
| `maxClaims=8 minYield=3` | 187.15 | 0.0 % | argmax |
