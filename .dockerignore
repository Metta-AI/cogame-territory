# The Dockerfile copies ONLY the pre-built, fully self-contained bundles
# (`dist-server/` from esbuild, `dist/` from vite), so everything else is context
# the daemon would upload for nothing — including ~9 MB of committed art raws.
*
!dist-server
!dist
